spring:
  application:
    name: stickee
  banner:
    location: classpath:banner.txt
  datasource:
    url: ${JDBC_URL}
  jpa:
    open-in-view: false
  web:
    resources:
      cache:
        cachecontrol:
          max-age: 365d
          cache-public: true
      chain:
        strategy:
          content:
            enabled: true
            paths: /**/*.css
